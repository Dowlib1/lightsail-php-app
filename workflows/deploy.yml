# name: Deploy to Lightsail via SSH

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Start ssh-agent and add key
#         uses: webfactory/ssh-agent@v0.8.1
#         with:
#           ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

#       - name: Copy repository to remote server (Lightsail instance)
#         env:
#           REMOTE_USER: ${{ secrets.REMOTE_USER }}
#           REMOTE_HOST: ${{ secrets.REMOTE_HOST }}
#           REMOTE_PATH: ${{ secrets.REMOTE_PATH }}
#         run: |
#           echo "Copying files to ${REMOTE_USER}@${REMOTE_HOST}:${REMOTE_PATH}"
#           # Create remote path and copy files
#           ssh -o StrictHostKeyChecking=no ${REMOTE_USER}@${REMOTE_HOST} "mkdir -p ${REMOTE_PATH} && sudo chown ${REMOTE_USER}:${REMOTE_USER} ${REMOTE_PATH}"
#           scp -o StrictHostKeyChecking=no -r . ${REMOTE_USER}@${REMOTE_HOST}:${REMOTE_PATH}

#       - name: Set permissions and restart Apache
#         env:
#           REMOTE_USER: ${{ secrets.REMOTE_USER }}
#           REMOTE_HOST: ${{ secrets.REMOTE_HOST }}
#           REMOTE_PATH: ${{ secrets.REMOTE_PATH }}
#         run: |
#           ssh -o StrictHostKeyChecking=no ${REMOTE_USER}@${REMOTE_HOST} "sudo chown -R www-data:www-data ${REMOTE_PATH} && sudo systemctl restart apache2"